{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "command -v enzyme >/dev/null 2>&1 || echo 'enzyme: binary not found. Install: curl -fsSL https://raw.githubusercontent.com/jshph/enzyme/main/install.sh | bash' >&2"
          }
        ]
      }
    ]
  }
}
